# NeuroScan AI backend
#
# torch is listed with no version pin on purpose: install the build that matches your
# machine (CPU-only, or CUDA-enabled if you have an NVIDIA GPU) by following
# https://pytorch.org/get-started/locally/ *before* running `pip install -r requirements.txt`,
# then this file will simply see torch already satisfied.

fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0
numpy>=1.26.0
scipy>=1.13.0
nibabel>=5.2.0
torch>=2.2.0
reportlab>=4.0.0
Pillow>=10.0.0
